import java.io.IOException;
import java.util.* ;

public class singleXorCypher {
    private static String ledu = "ETAOIN SHRDLUetaoinshrdlu";
    private static HashMap<Character, Integer> mapd = new HashMap<Character, Integer>();
    private static Boolean check(String f){
        for(int i=0; i< f.length();i++){
            if(f.charAt(i)>126 || f.charAt(i) < 32)
                return false;
        }
        return true;
    }

    public static  TreeMap<Character, Integer> cyph(String f){
        String a = hexTo64.hexDecode(f);
        boolean e ;
        int b = 0;
        for (int i = 0; i < a.length();i++){
            e = mapd.containsKey(a.charAt(i));

            if(!e)
                mapd.put(a.charAt(i),1);
            else{
                b = mapd.get(a.charAt(i));
                b += 1;
                mapd.put(a.charAt(i),b);
            }
        }
        TreeMap<Character, Integer> result = sortMap.sort(mapd);

        for(Map.Entry<Character,Integer> entry : result.entrySet()) {
            Character key = entry.getKey();
            Integer value = entry.getValue();
            int i = 0;
            for(i=0; i < ledu.length();i++){

                int q = (int)ledu.charAt(i) ^ (int)key;
                String t = fixedXor.xorSingle(a,q);
                if(check(t))
                    System.out.println(t);
            }
            break;
        }
        return result;
    }
    public static  void main(String args[]) throws IOException {

        cyph("1b37373331363f78151b7f2b783431333d78397828372d363c78373e783a393b3736");
//        System.out.println(mapd.get("a"));
    }
}
